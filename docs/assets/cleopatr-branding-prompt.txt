Mode: built-in image generation tool, edit/compositing.
Edit target: docs/assets/cleopatr-architecture.png (previous unbranded version).
Logo inputs: public/brand/cleopatr-logo.png and public/brand/cleopatr-feather.png.
Output: docs/assets/cleopatr-architecture.png.

Use case: compositing
Asset type: Cleopatr high-level architecture infographic, landscape.
Input images: Image 1 is the existing infographic EDIT TARGET. Image 2 is the exact supplied full logo: gold feather above gold lowercase cleopatr wordmark. Image 3 is the exact supplied shorthand gold feather icon.
Primary request: Update only the branding of Image 1 with the supplied logos. Replace the large upper-left navy Cleopatr heading with the COMPLETE full logo from Image 2, proportionally scaled into the ample upper-left region above the execution boundary. Preserve its feather, lowercase lettering, metallic gold treatment, shapes, and proportions as closely as possible. Keep the existing tagline "Central policy. Local control." beneath it, smaller if needed to fit. Use the small gold feather from Image 3 as the brand icon beside the mock web application's title and beside the "cleo" execution boundary heading. Preserve legibility and plenty of space. No black rectangles behind the logos; use their transparent cutouts on the infographic's light background.
Invariants: preserve the infographic's overall landscape composition and pale opaque background; preserve the control-plane panel, environment hierarchy, central Cedar policy vault, policy distribution arrow, local Cedar engine and offline policy cache. Preserve all four operation lanes exactly: GET /catalog → HTTP adapter → ALLOW → Approved API; DELETE FROM customers → Database adapter → BLOCKED before Customer database; rm -rf /workspace → Filesystem adapter → BLOCKED before Protected files; tools/call: search → MCP adapter → ALLOW → MCP tool server. Preserve the complete rounded execution enclosure around agent and adapters, the terminal "$ cleo run -- agent", green allowed arrows, red blocked barriers, purple policy distribution arrows, all labels and legend. Preserve the TARGET ARCHITECTURE badge and conceptual architecture footer. The signed policy bundle caption should read "Background refresh when older than 5 min" to reflect authorization-triggered refresh. No new claims of universal enforcement. Keep the bottom statement legible and unchanged. Do not redesign the supplied brand artwork or replace its lettering with a generic font. No extra objects or new decorative imagery.
