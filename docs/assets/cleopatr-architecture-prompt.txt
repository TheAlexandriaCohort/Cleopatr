Use case: infographic-diagram.
Create a polished, high-resolution landscape architecture infographic for Cleopatr (spell exactly C-l-e-o-p-a-t-r), an agentic AI rules harness. This is a conceptual target-architecture visual for a product presentation, not a screenshot of an existing app. Wide 16:9 canvas, ideally 3072x1728 or comparable high resolution. Professional clean vector-like editorial diagram, exceptionally readable text, sharp icons, restrained depth, generous whitespace. White / very pale cool-gray background, deep navy typography, violet Cleopatr control boundaries, teal-green allowed paths, coral-red blocked paths. Align the aesthetic with a sophisticated developer security product, not a cyberpunk poster.

Main title at upper left: "Cleopatr"
Subtitle: "Central policy. Local control."
Small top-right pill: "TARGET ARCHITECTURE"

Composition: a distinct CENTRAL CONTROL PLANE band at the top, and a much larger LOCAL AGENT RUNTIME section below. At top center/right, illustrate a small browser window labeled "Cleopatr web application", containing a visually legible simple nested environment tree "Organization > Production" and a policy vault with Cedar rule documents. Beside/in this window a vault/shield icon and title "Central policy vault". Tiny supporting line: "Author Cedar policies · Assign environments · Manage resources". From this vault, a clearly directional dashed violet arrow DOWN to a local engine/cache module below, labeled "Signed policy bundles". Near the dashed arrow: "Background refresh every 5 min". The dashed arrow distributes policies only, never carries agent traffic.

The main lower region: a large elegant rounded violet outline, visibly enclosing an AI agent and all its mediation components, labeled on its upper-left boundary "cleo — agent execution boundary". Do not draw a hardware enclave chip. Inside the left side put an appealing code/agent-program icon above a compact terminal card reading "cleo run -- agent". Label this node "Agentic program" and smaller "Agent + child processes". The outline must visibly surround the program, communicating the CLI has wrapped/enclosed its execution context.

From the enclosed program, fan out clean directional solid lines into FOUR evenly spaced horizontal operation lanes. Every lane flows left to right: action attempted -> corresponding adapter / local policy gate -> result. Place the adapters inside the violet execution boundary, with external resource icons outside it at far right. Keep lanes entirely distinct with no crossings. Use these EXACT operation labels and mappings:
1. "GET /catalog" -> "HTTP adapter" -> green check + "ALLOW" -> external resource labeled "Approved API".
2. "DELETE FROM customers" -> "Database adapter" -> red stop symbol + "BLOCKED" before it reaches an external resource labeled "Customer database".
3. "rm -rf /workspace" -> "Filesystem adapter" -> red stop symbol + "BLOCKED" before it reaches an external resource labeled "Protected files".
4. "tools/call: search" -> "MCP adapter" -> green check + "ALLOW" -> external resource labeled "MCP tool server".

Allowed paths have clear solid teal arrows that reach their resources. Blocked paths end at a red barrier BEFORE the protected resource; no continuing arrow, no red arrow that reaches the resource, no explosion or deletion. Resources behind the blocked gates remain intact and visually muted. Use simple consistent icons: API globe/brackets, database cylinder, file folder, tool-server blocks. Add compact rule explanations under adapter result areas: row 1 "Approved destination"; row 2 "Production deletes forbidden"; row 3 "Destructive removal forbidden"; row 4 "Tool permitted". Do not use code except the exact action and command strings.

Inside the execution boundary at bottom-center place one clearly distinct module labeled "Local Cedar engine" with smaller label "Verified policy cache". This receives the dashed download arrow from the central policy vault. Draw a fine violet decision connector spine from this engine to all four adapters/gates, showing all local decisions use the synchronized Cedar rules. Keep these policy connectors visually secondary to the horizontal action lanes. Place near engine a small offline icon and label "Last-known policies work offline".

Bottom caption in a clean single line: "Policies are managed centrally. Actions are authorized locally, before they reach resources."
Small unobtrusive footer: "Conceptual architecture • Adapter coverage varies by implementation and platform."
A compact legend can show "Action flow" as solid line and "Policy distribution" as dashed violet line.

Prioritize accurate architecture, obvious enclosure, beautiful hierarchy, unambiguous arrow directions and perfectly legible typography. No decorative robot mascot, no fake metrics, no gradients that wash out text, no crowded tangles, no vendor logos, no duplicate content. Do not imply traffic routes through the central web application. This is a conceptual desired architecture and not a claim that all depicted adapters are implemented today.

FINAL REFINEMENT (built-in image generation edit):

Edit this Cleopatr infographic with a single focused refinement: make the ENTIRE rectangular canvas fully opaque with a solid near-white background (#F8FAFD), including the title area, space between modules, and bottom footer. There must be NO transparent pixels, cutout background, black background, or alpha transparency anywhere. Re-render all dark navy title/annotation/footer text crisply against this opaque pale background for excellent readability. Preserve the existing architecture, all four action lanes, agent enclosure, green allow routes, red blocked stops, resource icons, central policy vault, and arrows. Keep 'Cleopatr', 'Central policy. Local control.', and 'TARGET ARCHITECTURE'. Remove the extra small marketing sentence below the subtitle ('Empower AI agents ...'); leave that area as clean whitespace. The sync annotation must read cleanly: 'Signed policy bundles' and below it 'Background refresh every 5 min' then '(policies only; no agent traffic)'. Bottom caption must be readable: 'Policies are managed centrally. Actions are authorized locally, before they reach resources.' Bottom-right small footer: 'Conceptual architecture • Adapter coverage varies by implementation and platform.' Everything should look like a polished finished architecture slide printed on an opaque white sheet. Landscape, preserve overall composition.